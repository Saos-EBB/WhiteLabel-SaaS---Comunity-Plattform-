import { Repository } from 'typeorm';
import { Profile } from './entities/profile.entity';
import { User } from '../auth/entities/user.entity';
import { Interest } from './entities/interest.entity';
import { UserInterest } from './entities/user-interest.entity';
import { AgbVersion } from './entities/agb-version.entity';
import { ConsentLog } from './entities/consent-log.entity';
import { ProfileSensitiveData } from './entities/profile-sensitive-data.entity';
import { Block } from './entities/block.entity';
import { UpdateProfileDto } from './dto/update-profile.dto';
import { SubmitSensitiveDataDto } from './dto/submit-sensitive-data.dto';
export declare class ProfileService {
    private readonly profileRepo;
    private readonly userRepo;
    private readonly interestRepo;
    private readonly userInterestRepo;
    private readonly agbVersionRepo;
    private readonly consentLogRepo;
    private readonly sensitiveDataRepo;
    private readonly blockRepo;
    constructor(profileRepo: Repository<Profile>, userRepo: Repository<User>, interestRepo: Repository<Interest>, userInterestRepo: Repository<UserInterest>, agbVersionRepo: Repository<AgbVersion>, consentLogRepo: Repository<ConsentLog>, sensitiveDataRepo: Repository<ProfileSensitiveData>, blockRepo: Repository<Block>);
    private encryptField;
    getOwnProfile(userId: string): Promise<Profile>;
    updateOwnProfile(userId: string, dto: UpdateProfileDto): Promise<Profile>;
    private checkAndCompleteOnboarding;
    publishProfile(userId: string): Promise<Profile>;
    getInterests(): Promise<Interest[]>;
    getUserInterests(userId: string): Promise<UserInterest[]>;
    addInterest(userId: string, interestId: string): Promise<UserInterest[]>;
    removeInterest(userId: string, interestId: string): Promise<UserInterest[]>;
    getPublicProfile(nickname: string): Promise<Partial<Profile>>;
    searchProfiles(requestingUserId: string, city?: string, interestIds?: string[]): Promise<Partial<Profile>[]>;
    createSensitiveDataConsent(userId: string, ip: string): Promise<Partial<ConsentLog>>;
    submitSensitiveData(userId: string, dto: SubmitSensitiveDataDto): Promise<{
        disability_visible: boolean;
        collected_at: Date;
    }>;
    blockUser(blockerId: string, blockedId: string): Promise<void>;
    unblockUser(blockerId: string, blockedId: string): Promise<void>;
}
